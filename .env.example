# Database Configuration
# PostgreSQL connection string format: postgresql://username:password@host:port/database?sslmode=disable
DATABASE_URL=postgresql://mep:password@127.0.0.1/suomisf?sslmode=disable

# Or individual components (if using separate env vars)
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=mep
DB_PASSWORD=password
DB_NAME=suomisf

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Server Configuration
PORT=8088
GIN_MODE=debug

# CORS Configuration (optional)
# CORS_ORIGINS=http://localhost:3000,http://localhost:8080
